<%= render 'shared/title',title:'Post List'%>
<% @posts.each do |post|%>
	<div class="row">
	  <div class="well col-sm-4">
		  <p><%=post.title%></p>
		  <p><%= post.content %></p>
		  <%= link_to "show post", post_path(post)%>
	  </div>
	 </div>
<%end%>